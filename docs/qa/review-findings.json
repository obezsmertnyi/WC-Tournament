{
  "reviewedAt": "2026-07-01",
  "reviewers": [
    "scoring-correctness-reviewer (separate agent)",
    "security-reviewer (separate agent)",
    "CodeRabbit (external, on the PR)"
  ],
  "note": "maker != checker: findings raised by independent reviewers that did not write the code. clean=true means no unresolved critical/high CODE defect; S1 is an operational secret-hygiene action for the owner (not a repo defect).",
  "clean": true,
  "findings": [
    { "id": "C1", "area": "recap", "severity": "critical", "status": "fixed",
      "issue": "validateRecap team check was a no-op on the live path (MatchRecap passed no knownTeams), so a hallucinated team could pass if its digits were grounded.",
      "fix": "recap() now fail-safes: a non-grounded (custom/LLM) provider without a team registry never displays — it falls back to the grounded template. Live path uses the grounded generator (cannot hallucinate). Registry-based team grounding is required and tested for the future LLM provider (FR-082)." },
    { "id": "C2", "area": "recap", "severity": "high", "status": "fixed",
      "issue": "factNumbers() mixed stage-label digits into the allowed set (R16→16, R32→32), letting a fabricated number equal to the stage slip past.",
      "fix": "Number grounding is scoreline-only; stage labels are now digit-free (e.g. 'the last sixteen')." },
    { "id": "C3", "area": "recap", "severity": "medium", "status": "accepted",
      "issue": "Injection detection is a phrase blocklist that a real LLM could evade.",
      "fix": "Low blast radius today (grounded-only provider; raw output never shown; React escapes the text node). When FR-082 adds an LLM provider, replace the blocklist with strict allow-model grounding. Documented in ADR-0016." },
    { "id": "C4", "area": "traceability", "severity": "high", "status": "fixed",
      "issue": "gen-traceability counted @trace in ANY source file (comments/strings), so coverage could be faked and the ratchet would lock in fiction.",
      "fix": "Scanner now only reads test/eval files (*_test.go, *.test.*, *.eval.*); a @trace in ordinary source no longer counts. Coverage held at 17/19 after the change." },
    { "id": "C5", "area": "bracket", "severity": "medium", "status": "fixed",
      "issue": "Cycle guard (depth>8) could silently, order-dependently mis-order a malformed feeder graph.",
      "fix": "Added a per-path `seen` set (deterministic: a repeat is treated as a root) and tightened MAX_DEPTH to the real tree height (5)." },
    { "id": "C6", "area": "scoring", "severity": "low", "status": "fixed",
      "issue": "scoring/spec.md attributes FR-014 (bonus) to CAP-02 while it is implemented/traced in internal/resolve.",
      "fix": "Added a locality note to scoring/spec.md; FR-014 is verified by resolve_test.go where it lives." },
    { "id": "C7", "area": "scoring", "severity": "info", "status": "clean",
      "issue": "Score() probed against all spec scenarios — no off-by-one/sign/nil/purity break found." },
    { "id": "S1", "area": "secrets", "severity": "high", "status": "action-required-owner",
      "issue": "The on-disk .env holds real production credentials (Telegram token, Google OAuth secret, JWT_SECRET, ADMIN_PASSWORD). It is gitignored and absent from git history (verified) — not a repo leak — but the values were exposed to review tooling.",
      "fix": "OWNER ACTION: rotate all four (BotFather revoke, GCP OAuth secret rotate, regenerate JWT_SECRET + ADMIN_PASSWORD). No history scrub needed." },
    { "id": "S2", "area": "secrets", "severity": "info", "status": "clean",
      "issue": ".env gitignored, never tracked, absent from history; no real secret value in any tracked file/test/fixture/CI." },
    { "id": "S3", "area": "mcp", "severity": "info", "status": "clean",
      "issue": "All 5 tools read-only; strict schemas reject unknown params + bounds before fetch; no credential held/forwarded; reveal-lock enforced." },
    { "id": "S4", "area": "access", "severity": "info", "status": "clean",
      "issue": "Fail-closed JWT_SECRET; DemoGate ordering gives anon 401 not 403; ro/rw gating correct; new Google users default 'none' under demo mode." },
    { "id": "S5", "area": "recap", "severity": "info", "status": "clean",
      "issue": "Recap rendered as an escaped React text child; no dangerouslySetInnerHTML; guardrail + fallback are defense-in-depth." },
    { "id": "S6", "area": "ci", "severity": "info", "status": "clean",
      "issue": "No untrusted github.event.* in run: blocks; least-privilege permissions; gitleaks full-history; Trivy fails HIGH/CRITICAL." }
  ]
}
