# Current state — fast handoff

> One-screen resume so a fresh agent session continues in ~30s. Update at the
> end of each slice. Detail lives in `docs/memory/`.

**Task:** agentic-engineering augmentation of WC-Tournament for the fwdays
Greenfield homework. Driven by `CHECKLIST.md`.

**Branch:** `main` (consolidated; default). App live in prod.

**Just finished:** Phase A — context engineering (`CLAUDE.md`, `AGENTS.md`,
`docs/memory/`, this file).

**Doing next:** Phase C — SDD (`docs/requirements.md` FR/NFR/TC/BC →
`docs/mvp-capability-plan.md` → `docs/features/<cap>/spec.md` Given/When/Then →
ADR-0013 context-architecture, ADR-0014 requirement-id grammar).

**Then:** D verification (evals + frontend vitest + traceability) → G MCP server
→ E maker≠checker → F loop docs → B narrative + uk PR body → H ship + handoff.

**Watch-outs:** no `gh` (hand off GitHub steps); keep `main` green; docs EN /
PR uk; don't change scoring/access behavior without an ADR.
