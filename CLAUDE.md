# CLAUDE.md — WC-Tournament

Project rules live in **`AGENTS.md`** (single source of truth, shared with Codex
and other tools). Claude Code loads them via the import below — do not duplicate
rules here (avoids the CLAUDE.md/AGENTS.md drift the team hit when Codex exported
a copy).

@AGENTS.md
